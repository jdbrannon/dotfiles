[general]
framerate = 60
bars = 0
autosens = 1
sensitivity = 100

[input]
method = pulse
source = auto

[output]
method = ncurses
channels = stereo

[smoothing]
noise_reduction = 60

# Colors regenerated by matugen into cava/themes/matugen; see hypr/scripts/theme-switcher.sh
theme = 'matugen'

